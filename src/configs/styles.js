import {system} from './systemColors.js'
export const styles = {
    splash: {
        backgroundColor: system.priColor
    },
    custom: {
        border: `1px solid ${system.comColor}`,
        color: system.comColor,
        backgroundColor: "white",
        fontWeight: "700"
    },
    button: {
        backgroundColor: system.priColor,
    }
}