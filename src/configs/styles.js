import {system} from './systemColors.js'
export const styles = {
    splash: {
        backgroundColor: system.priColor
    },
    Form: {
        
    },
    button: {
        backgroundColor: system.priColor
    }
}